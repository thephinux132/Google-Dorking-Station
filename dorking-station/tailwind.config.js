/** Tailwind CSS configuration (v4-compatible)
 * - Enables class-based dark mode to match the app toggling `html.dark`
 */
export default {
  darkMode: 'class',
  theme: {
    extend: {},
  },
  // v4 does not require content globs; leaving them out intentionally
}

