import GoogleDorkingStation from './GoogleDorkingStation';
import './index.css'; // Make sure the CSS is imported

function App() {
  return (
    // The 'dark' class on the root HTML element will be managed by your component
    <GoogleDorkingStation />
  )
}

export default App